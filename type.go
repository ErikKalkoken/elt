package main

import (
	"context"

	"github.com/urfave/cli/v3"
)

func types(ctx context.Context, cmd *cli.Command) error {
	// if cmd.Args().Len() == 0 {
	// 	return cli.Exit("no IDs provided", 1)
	// }
	// r, err := retryablehttp.Get("https://" + path.Join(esiBaseURL, "universe", "types"))
	return nil
}
